---
description: Boss JD Copilot 产品、目录结构与文件落点（始终生效）
alwaysApply: true
---

# Boss JD Copilot

完整说明见 **AGENTS.md** §3。新建/修改代码前**必须先对表落点**，禁止在错误目录新建文件。

## 产品（不可违背）

- 面向**求职者**：JD → AI 个性化招呼语，**不离开 Boss 页**填入/发送。
- 非目标：批量爬取、自动化投递、招聘方能力。
- **仅三页注入**：
  - `zhipin.com/job_detail*` — 独立详情页，抽 JD + 缓存
  - `zhipin.com/web/geek/jobs*` — 推荐列表（左列表 + 右详情面板），右侧 DOM 同详情页，抽 JD + 缓存；用户在左列表切换卡片时右侧会替换，observer 需持续监听
  - `zhipin.com/web/geek/chat*` — 匹配缓存 + 填入输入框

  其它 Boss 页（个人中心、消息、`gongsi/*` 等）不注入。

## 目录树（权威）

```text
boss-jd-copilot/
├── popup.tsx              # 仅：弹窗壳、状态、跳转设置、开发探针
├── options/               # 选项页（已折叠为文件夹形式）
│   ├── index.tsx          #   入口：state + save
│   ├── ui.tsx             #   Section/Field/styles
│   ├── ProviderSection.tsx
│   ├── ProfileSection.tsx #   textarea + PDF 上传（解析完直接覆盖 textarea）
│   └── GreetingSection.tsx#   tone 预设 + 自定义 prompt 编辑器
├── background.ts          # 仅：消息路由、AI 请求、storage 编排
├── content.ts             # 仅：CS 入口、matches、挂载 UI、调用 lib
├── contents/              # ⚠️ Plasmo 把这里所有 .tsx 都注册为 CSUI 入口
│   └── boss-panel.tsx     #   CS 入口 + 容器壳 + 拖动 + 探针 state（helper 一律走 lib/ui/）
├── lib/                   # 所有可复用逻辑（默认新建在这里）
│   ├── boss/
│   │   ├── pages.ts       # URL → 页面类型
│   │   ├── dom-job-detail.ts
│   │   ├── dom-chat.ts
│   │   └── probe.ts       # DOM 探针（绕过 Boss 反调试用）
│   ├── storage/
│   │   ├── keys.ts
│   │   └── job-cache.ts
│   ├── ai/
│   │   ├── client.ts
│   │   └── prompts.ts
│   ├── ui/
│   │   └── boss-panel/    # 浮层子组件（非 CS 入口，被 contents/boss-panel/index.tsx 引用）
│   │       ├── ui.tsx     #   共享样式 / 图标 / IS_DEV / requestOpenOptions
│   │       ├── JobDetailView.tsx
│   │       ├── ProbeView.tsx  # 纯展示
│   │       └── ChatPlaceholder.tsx
│   ├── messages.ts
│   └── debug.ts
├── assets/                # 图标等静态资源
├── AGENTS.md
├── build/                 # ❌ 禁止手改、禁止新建源码
└── .plasmo/               # ❌ 禁止手改
```

## 新建文件落点表（必查）

| 要写的内容 | 创建位置 | 禁止放在 |
|------------|----------|----------|
| Plasmo 扩展入口（manifest 能力） | 根目录 `popup.tsx` / `options.tsx` / `background.ts` / `content.ts` | `lib/`、`build/` |
| Boss 页面内浮层 CS 入口 | `contents/<name>/index.tsx`（folder 只放 index） | 同目录塞 helper（Plasmo 会把 `contents/<name>/` 下所有 .tsx 都注册成 CSUI 入口，没 default export 的会在非 Boss 页报"Element type is invalid"） |
| 浮层子组件 / 共享 UI 片段 | `lib/ui/<feature>/` | `contents/<name>/` 子文件 |
| URL 识别、页面类型 | `lib/boss/pages.ts` | `content.ts` 内联 |
| 详情页/聊天页 DOM 选择器与抽取 | `lib/boss/dom-job-detail.ts`、`dom-chat.ts` | 其它目录 |
| 找选择器（Boss 不让开 F12） | `lib/boss/probe.ts` + 浮层探针按钮 | 依赖 Boss 控制台 |
| `chrome.storage` key 与读写 | `lib/storage/` | content / popup |
| 扩展内消息类型 | `lib/messages.ts` | 各文件散落定义 |
| 大模型 API、prompt | `lib/ai/` + 调用方 `background.ts` | `content.ts`、`popup.tsx` |
| 调试上报 | `lib/debug.ts` | Boss 页 `console` 依赖 |
| 共享 UI 片段（非 CS 入口） | `lib/ui/`（需要时建） | 复制到 popup 与 contents |

根目录入口文件保持**薄**：`import` from `~lib/...`，不写长 DOM 字符串、不写 `fetch` API Key。

## 技术要点

- Plasmo + Chrome MV3 + React + TS + pnpm；`pnpm dev` → 加载 `build/chrome-mv3-dev`。
- 改 `content.ts` 的 `matches` 后须在 `chrome://extensions` **刷新扩展**。
- DOM 优先；密钥仅 `options` + `background`。
- import 使用 `~lib/boss/pages`（`~*` = 项目根，见 `tsconfig.json`）。
- **Host permissions**：常驻只列 `https://*.zhipin.com/*`；AI baseURL 走 `optional_host_permissions` + options 页 user-gesture 同步调 `chrome.permissions.request`。新增外部 fetch 域名沿用同一套，**禁止把常驻权限放宽回 `https://*/*`**（商城审核必拦）。

## 编码

- 最小 diff；用户确认后再填入发送；不提交 `build/`、`.plasmo/`。
